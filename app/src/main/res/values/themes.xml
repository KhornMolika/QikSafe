<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- ✅ Splash Screen Theme (Backward-compatible) -->
    <style name="Theme.QikSafe.Splash" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/white</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/white</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <!-- ✅ Main App Theme -->
    <style name="Base.Theme.TestFeatureOfQikSafe" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorControlNormal">@color/primary</item>
        <item name="colorPrimary">@color/primary</item>
    </style>

    <style name="Theme.TestFeatureOfQikSafe" parent="Base.Theme.TestFeatureOfQikSafe" />

</resources>

